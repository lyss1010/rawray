========================================================================
    STATIC LIBRARY : math Project Overview
========================================================================

The math library contains basic classes like vectors, matricies along with
common operations on these objecsts. Operator overloading is used wherever
possible to ease in using these classes.

/////////////////////////////////////////////////////////////////////////////
