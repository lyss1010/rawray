========================================================================
    STATIC LIBRARY : geometry Project Overview
========================================================================

The geometry library contains classes to define basic shapes such as
triangles, boxes, shperes, etc... Each class should contain code 
intersection code.

/////////////////////////////////////////////////////////////////////////////
